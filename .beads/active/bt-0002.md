---
id: bt-0002
status: in_progress
updated: 2026-05-13
scope_files:
  - Data/CoreDataManager.swift
  - Data/CoreDataManager+HabitDetails.swift
  - Data/Models.swift
  - Data/DataStore.swift
  - Data/BackupManager.swift
  - Data/BackupRestoreViewModel.swift
  - App/Bullet_TrackerApp.swift
  - App/ContentView.swift
  - App/SettingsView.swift
  - Habits/HabitDataRepository.swift
  - Habits/TodayView.swift
  - Habits/TodayViewModel.swift
  - Habits/TodayHabitCardView.swift
  - Habits/HabitCompletionDetailView.swift
  - Habits/HabitDashboardView.swift
  - Habits/HabitDashboardViewModel.swift
  - Habits/HabitDetailDashboardView.swift
  - Habits/EditHabitView.swift
  - Habits/HabitFormView.swift
  - Habits/AddHabitView.swift
  - Utilities/HabitCalculations.swift
  - BulletTrackerWidgets/BulletTrackerWidgetsBundle.swift
  - BulletTrackerWidgets/WidgetEnums.swift
  - Journal/DayJournalView.swift
  - Journal/JournalExportView.swift
  - Journal/JournalJSONExporter.swift
  - Journal/JournalPDFGenerator.swift
  - Notes/NotesView.swift
  - "Bullet Tracker/Bullet_Tracker.xcdatamodeld/Bullet_Tracker.xcdatamodel/contents"
  - "Bullet Tracker.xcodeproj/project.pbxproj"
blocked_by: null
---

# SwiftData migration — Phase 1: swap the engine, model shape unchanged

## Why

`bt-0001` decided it (2026-05-12): migrate the data layer off hand-rolled Core Data onto SwiftData. Phase 1 is the engine swap with the **model shape unchanged** — convert the 6 entities to SwiftData `@Model` types that mirror the current `.xcdatamodeld` field-for-field, so SwiftData coexists with Core Data on the existing App-Group / CloudKit store while consumers migrate area-by-area; the Core Data stack is deleted last. Nothing user-visible ships from Phase 1. The wins land when it's done: flags #1 (three doors), #7 (cache thrash), #8 (calc thread-safety) gone rather than patched; flag #4 (name-keyword workout detection) folded in; a big chunk of hand-rolled plumbing deleted.

Phase 2 (separate later bead) restructures the model — `details` JSON blob → typed, 3-way completion state → one enum, drop vestigial `JournalEntry` fields (blocked on the #9 intent question). Out of scope here.

## Out of scope

- The model *shape* — no entity/attribute restructuring in Phase 1 (that's Phase 2). The `@Model` types mirror the existing `.xcdatamodeld` exactly so coexistence works.
- Repo-layout cleanup (flag #5) — independent, not part of this.
- Any UI / behavior change. Phase 1 is invisible to the user.

## Plan — one focused data-layer push on the `swiftdata-migration` branch

**Strategy (decided 2026-05-12, approach "(b)"):** no Core-Data-coexistence period (the dual-model-sync path was rejected as too fragile for a one-person app). Do the whole data-layer swap in one concentrated effort on a branch; the app won't build/run for the duration; JSON backup (exported from Settings before testing) is the safety net; merge when it compiles + works.

**Data migration:** plan for **in-place open** of the existing `BulletTracker.sqlite` (App Group) with a SwiftData `Schema` that exactly matches what Core Data wrote — that's why `Data/Models.swift` mirrors the `.xcdatamodeld` field-for-field. Fallback if first-launch testing on a *copy* of real data shows problems: migrate to a fresh store (read the old one with a throwaway `NSPersistentContainer` using the kept `.xcdatamodeld`, copy everything into a new SwiftData store, retire the old one) — accepting a one-time CloudKit re-sync.

**CloudKit:** the SwiftData `ModelContainer` is configured `cloudKitDatabase: .private("iCloud.db23.BulletTracker")` on the same App Group store; schemas match → CloudKit record types match → sync continues. Verify on a second device / let it settle.

**Codegen:** `.xcdatamodeld` entities' Codegen → **Manual/None** (then the file is deleted at the end — `NSPersistentCloudKitContainer(name:"Bullet_Tracker")` is gone). The `@Model` classes keep the names `Habit`/`HabitEntry`/etc. (no prefix needed — nothing collides once Core Data is removed).

**Commit-stages within the branch** (not separately shippable — checkpoints, not releases):
1. `Data/Models.swift` compiles clean (Xcode pass — `@Model` schema + relationship-inverse fixups); `.xcdatamodeld` Codegen → Manual/None; `Models.swift` re-added to app + widget targets.
2. `Data/DataStore.swift` (new) — the shared `ModelContainer` (App Group URL, `cloudKitDatabase: .private(…)`), `mainContext`, a `ModelActor` for background writes. Wire `.modelContainer(…)` into `App/Bullet_TrackerApp.swift`. Delete `CoreDataManager` + `CoreDataManager+HabitDetails` (or stub during the push, delete in stage 7).
3. Habits views/viewmodels off `HabitDataRepository` → `@Query` / `FetchDescriptor` + `modelContext`. Delete the cache dict / `loadEntries` / `loadedDateRange` / `clearCache` / batch-load / optimistic-update bookkeeping / `NSManagedObjectContextDidSave` listener. Keep `getCompletionState`/`HabitCompletionState` (as plain helpers), `reorderHabits`, post-write `WidgetCenter.reloadTimelines`. Files: `HabitDataRepository`, `TodayView`/`TodayViewModel`/`TodayHabitCardView`/`HabitCompletionDetailView`/`HabitDashboardView`/`HabitDashboardViewModel`/`HabitDetailDashboardView`/`EditHabitView`/`HabitFormView`/`AddHabitView`. Fold in **flag #4** (kill name-keyword "is this a workout?", use `detailType`). → resolves #1, #7, #4.
4. Widget — rewrite `CompleteHabitIntent` + `HabitWidgetProvider` against a `ModelContainer` constructed in the widget extension (same App Group store + same `Schema`); widget views untouched; revisit `WidgetEnums.swift` stubs.
5. `HabitCalculations.swift` — keep the streak/rate/heatmap math as pure functions over `[HabitEntry]`; drop the stored `viewContext` + `using ctx:` params + `@unchecked Sendable`. → resolves #8.
6. Journal/Notes/Settings — mechanical `NSFetchRequest`/`CoreDataManager` → `@Query`/`FetchDescriptor` in `DayJournalView`, `JournalExportView`, `JournalJSONExporter`, `JournalPDFGenerator`, `NotesView`, `SettingsView`. `BackupManager` — `Codable` mirror structs + file format unchanged; read → `FetchDescriptor`, write → `obj.init()` + `modelContext.insert`. `BackupRestoreViewModel` likely untouched.
7. Delete Core Data leftovers — `.xcdatamodeld`, any remaining `import CoreData`, `CoreDataManager*` if not already gone. Build clean. Then **test:** fresh backup → run on device → existing habits/entries/journal/notes show → CloudKit syncs → widget works → backup export/restore round-trips. Then merge to `main`.

**Dustin's Xcode touchpoints** (kept minimal — the bulk is Claude writing Swift): branch created (done); export a JSON backup before testing; at the end — build + fix-with-Claude, run the test checklist.

**Watch items during the migration:**
- The `.xcdatamodeld` had fetch indexes on `HabitEntry.date` and `JournalEntry.date` (named `byDateIndex`). `Models.swift` doesn't recreate them yet — add `#Index<HabitEntry>([\.date])` / `#Index<JournalEntry>([\.date])` to the `@Model` types before merge (perf, not correctness — but the dashboard heatmap / day-journal queries hit `date` ranges hard).
- `ModelConfiguration(schema:url:cloudKitDatabase:.automatic)` and the App-Group `storeURL` are unverified against this SDK / against real data — if the build rejects that init signature, or first-launch testing on a data copy shows the store won't open, switch to the "(b2)" fallback (read old store with a throwaway `NSPersistentContainer` using the kept `.xcdatamodeld`, copy into a fresh SwiftData store, accept a one-time CloudKit re-sync).
- `Color(hex:)` is defined in both `Utilities/Color+Hex.swift` and `BulletTrackerWidgetsBundle.swift` (pre-existing duplication, not our doing) — leave it; just don't trip over it.

## Decision log (cont.)

- 2026-05-12 — **migration start state confirmed** on the `swiftdata-migration` branch: `.xcdatamodeld` Codegen → Manual/None on all 6 entities, `Models.swift` in both targets, build is red with exactly the expected shape — `'Habit'/'Collection'/etc. has no member 'fetchRequest'`, `does not conform to NSFetchRequestResult`, `cannot convert ... to NSManagedObject`, `extra argument 'context' in call` — i.e. the un-migrated Core Data code can't see the old generated classes anymore (no "ambiguous" errors left). That's the work surface. Migration code-writing begins next, in waves (build stays red until the last wave): (1) `DataStore.swift` + `Bullet_TrackerApp` wiring + retire `CoreDataManager`; (2) `HabitDataRepository` + Habits consumers + flag #4; (3) `HabitCalculations`; (4) Journal/Notes/Settings + `BackupManager`; (5) widget; then build → iterate on remaining errors → test → merge.
- 2026-05-12 — **Wave 1 done** (committed `37045dc` is the baseline; Wave 1 code is uncommitted on the branch as of session end). `Data/DataStore.swift` written + added to both targets: `enum DataStore` exposing `schema` (the 6 `@Model` types), `storeURL` (`<AppGroup group.db23.Bullet-Tracker>/BulletTracker.sqlite`), and `shared: ModelContainer` built from `ModelConfiguration(schema:url:cloudKitDatabase:.automatic)` — in-place open of the existing CloudKit store (the "(b1)" path; "(b2) migrate to fresh store" is the fallback if device testing on a data copy shows the open fails). `App/Bullet_TrackerApp.swift` slimmed 34→17 lines: removed the `CoreDataManager` + `HabitDataRepository` env wiring, installed `.modelContainer(DataStore.shared)`. Both compiled clean (not in the error list — only `CoreDataManager` errors show because the compiler bails there before reaching the other un-migrated files). `CoreDataManager.swift` / `CoreDataManager+HabitDetails.swift` left intentionally broken — deleted in the final wave once nothing references them. **Next session = Wave 2:** rewrite `HabitDataRepository` (keep `getCompletionState`/`HabitCompletionState` as plain helpers, `reorderHabits`, the post-write `WidgetCenter.reloadTimelines`; delete the cache dict / `loadEntries` / `loadedDateRange` / `clearCache` / batch-load / optimistic-update bookkeeping / `NSManagedObjectContextDidSave` listener) → `@Query` in the views; convert `TodayView`/`TodayViewModel`/`TodayHabitCardView`/`HabitCompletionDetailView`/`HabitDashboardView`/`HabitDashboardViewModel`/`HabitDetailDashboardView`/`EditHabitView`/`HabitFormView`/`AddHabitView`; fold in flag #4 (kill name-keyword "is this a workout?", use `detailType`). Chunk it (Today tab → Dashboard/Detail → Edit/Add forms). Files not yet read by Claude (read them first next session): `TodayView`, `TodayViewModel`, `TodayHabitCardView`, `HabitCompletionDetailView`, `HabitDashboardView`, `HabitDashboardViewModel`, `HabitDetailDashboardView`, `EditHabitView`, `HabitFormView`, `AddHabitView`, `ContentView`, `HabitCalculations`.

- 2026-05-12 — **Wave 2 spec locked** (Dustin confirmed "go with your recs"). Reordered: **Wave 2 now also folds in Wave 3** (`HabitCalculations.swift`) because the Habits dashboard VMs depend on it — natural unit. Sub-chunks: (2.1) `Utilities/HabitCalculations.swift` → drop `@unchecked Sendable` + stored `viewContext` + `private init` + `using ctx:` params + the Core-Data fetch overloads (`fetchEntries`/`fetchAllEntries`/no-arg `calculateCurrentStreak(for:)`/`isHabitCompleted` and the convenience `calculateBestStreak`/`calculateCompletionRate`/`calculateOverallCurrentStreak`); keep the pure math over `[Date:HabitEntry]`/`[HabitEntry]`; replace the deleted fetch overloads with `…(in context: ModelContext)` variants where the dashboard VMs need them — resolves flag #8. (2.2) `Habits/HabitDataRepository.swift` repurposed: delete the cache dict / `loadEntries` / `loadedDateRange` / `clearCache` / `forceRefresh` / `invalidateCache` / `performBatchLoad` / `performCoreData*` / `loadHabits` / the `NSManagedObjectContextDidSave` listener; keep `HabitCompletionState` struct; replace `HabitDataRepository` (the `@Observable` singleton) with `enum HabitStore` of static funcs — `entry(for:on:)` (filters `habit.entries`), `completionState(for:on:)`, `setCompletion(for:on:state:in:)`, `removeEntry(for:on:in:)`, `updateEntryDetails(for:on:state:details:in:)`, `reorderHabits(_:to:in:)` — each write does `WidgetCenter.reloadTimelines(ofKind: "HabitTrackerWidget")` if the date is today; fold flag #4 (`isWorkoutHabit` = `detailType == "workout"` only, drop the name-keyword guess). (2.3) Today tab — `TodayView`/`TodayViewModel`/`TodayHabitRowView`/`HabitCompletionDetailView`: view owns `@Query(sort:\Habit.order) var habits` + `@Environment(\.modelContext)`; VM keeps `selectedDate`/sheet state, its `habits`-dependent computed props become funcs taking `[Habit]` (or move to the view); drop all `dataRepository.clearCache()` calls (`@Query` is live). (2.4) Dashboard — `HabitDashboardView`/`HabitDashboardViewModel`/`HabitDetailDashboardView`(+`HabitDetailViewModel`): VMs take a `ModelContext`, fetch entries via `FetchDescriptor<HabitEntry>` (predicate on `date` range), call the pure calc funcs; **main-thread fetch+calc for Phase 1** (no `ModelActor` — revisit with `#Index<HabitEntry>([\.date])` if it janks). (2.5) Forms — `HabitFormViewModel`/`EditHabitView`/`AddHabitView`/`HabitFormView`: VM takes `ModelContext`, does `modelContext.insert(Habit(...))` / mutate / `modelContext.delete(habit)` instead of `CoreDataManager.shared.{create,update,delete}Habit`. `ContentView`/`DashboardComponents` need no changes. Checkpoint commit after 2.2 and after each tab. App stays red until the last wave (Journal/Notes/Settings/Backup + widget + delete `CoreDataManager*`/`.xcdatamodeld`).

- 2026-05-12 — **Wave 2 done** (commits `87990d7` 2a, `c5b76ab` 2b, `194f02e` 2c on the branch — checkpoints, not releases). Compile-checked by reading only; no Xcode build run yet (build stays red until the last wave). Done: (2.1) `Utilities/HabitCalculations.swift` 479→~330 lines — `HabitCalculationService` is now a stateless `final class` (no `@unchecked Sendable`, no stored context); pure math over pre-fetched entry collections; `calculateBatchStats`/`buildHeatmapData` take an `allEntries` dict instead of fetching; deleted the Core-Data fetch overloads (this breaks `Journal/JournalPDFGenerator.swift` which still calls `fetchAllEntries` — fixed in the Journal wave). (2.2) `Habits/HabitDataRepository.swift` 494→~190 lines — `@Observable` singleton + cache + save-notification listener gone; `enum HabitStore` has read helpers over `habit.entries` (`entry(for:on:)`, `completionState(for:on:)`, `entriesByDay`, `allEntriesByDay`) and write helpers over a `ModelContext` (`setCompletion`, `removeEntry`, `updateEntryDetails`, `reorderHabits`), each write does `WidgetCenter.reloadTimelines(ofKind:"HabitTrackerWidget")` if the date is today; `HabitCompletionState` struct kept; **flag #4 folded** (`isWorkoutHabit` = `detailType == "workout"` only). (2.3) Today tab — `TodayView` owns `@Query(sort:[\.order,\.name]) var habits` + `@Environment(\.modelContext)`; `TodayViewModel` keeps `selectedDate`/sheet state, habit-derived values are funcs over a passed-in `[Habit]`, day-nav just sets `selectedDate` (no cache calls); `TodayHabitRowView` + `HabitCompletionDetailView` use `HabitStore` + `modelContext` directly; dropped the `.environment(dataRepository)` injection and the scenePhase-clearCache. (2.4) Dashboard — `HabitDashboardView` `@Query var habits`, recompute stats on appear + on period change (dropped `hasLoadedOnce` + scenePhase); `HabitDashboardViewModel.loadData(habits:)` pulls entries via `HabitStore.allEntriesByDay`, runs the pure calc on the main actor, dropped the dead heatmap/isLoading props; `HabitDetailDashboardView`/`HabitDetailViewModel` — no more background Core-Data context, `loadData` reads `HabitStore.entriesByDay` + pure calc. (2.5) Forms — `HabitFormViewModel.saveHabit(in:)`/`deleteHabit(in:)` insert/mutate/delete on a passed-in `ModelContext` (new-habit `order` via `fetchCount`, delete relies on the `.cascade` rule), no longer async; `EditHabitView`/`AddHabitView` pass `@Environment(\.modelContext)`; `HabitFormView` dropped unused `import CoreData`. `ContentView`/`DashboardComponents.swift` untouched (no CD refs). **Open watch-items carried forward:** (a) `@Query`'s `[Habit]` array identity doesn't change when a child `HabitEntry` is added/removed (Habit is a reference type) — so the Dashboard recomputes on every `.onAppear` rather than reactively. No real scaling cliff (habit-tracker data is bounded); leaving as-is. (b) widget cross-process writes — the old `NSManagedObjectContextDidSave` merge listener is gone; SwiftData's main-context auto-merge from CloudKit should cover it, verify during device testing. (c) `#Preview` blocks in the dashboard views will crash in the canvas (no `.modelContainer`) — cosmetic, ignored for Phase 1. **(d) DEFERRED PERF FIX — dashboard calc on the main actor.** Wave 2 dropped the old background-Core-Data-context dance; the dashboard aggregation now runs on `@MainActor`. Fine at current data sizes; can hitch a frame or two on the year view after years of dense data. Fix later: move the calc into a SwiftData `ModelActor` (~same shape as the old background context, ~30 lines). **(e) DEFERRED PERF FIX — `HabitStore` reads filter `habit.entries` in memory.** `entry(for:on:)` / `entriesByDay` / `allEntriesByDay` pull a habit's *entire* entries relationship into memory and filter by date in Swift, rather than a date-predicated fetch with a limit (what the old Core Data code did). For a habit tracked daily for years that's ~hundreds-to-thousands of objects faulted per habit per Today-list render. Fix later: `FetchDescriptor<HabitEntry>` with `#Predicate` on `habit.id` + date range (dodged in Wave 2 because `#Predicate` over optional relationships is fiddly, but doable). — **(d)+(e) decision deferred to Phase-1-ship time:** either a small `bt-0004` "SwiftData data-layer perf pass" bead, or fold into the Phase 2 bead (`bt-0003`) since it's already touching the data layer. Per `feedback_when_to_bead` this is real design content (tradeoffs to record), so it earns *a* bead — just not yet. **Next session = Wave 3 (was Wave 4):** Journal/Notes/Settings + `BackupManager` (`DayJournalView`, `JournalExportView`, `JournalJSONExporter`, `JournalPDFGenerator`, `NotesView`, `SettingsView`, `BackupManager`, `BackupRestoreViewModel`).

- 2026-05-13 — **Wave 4 done** (commit `4268ec0`; compile-checked by reading only). `BulletTrackerWidgets/BulletTrackerWidgetsBundle.swift` 627→612 lines (−15; net shrinkage from removing Core Data ceremony — `context.perform { } / DispatchQueue.main.async { }` queue dance, the `Set<HabitEntry>` cast, the explicit `NSFetchRequest` builders — partly offset by an 8-line header doc comment). `BulletTrackerWidgets/WidgetEnums.swift` 68→70 (+2; refreshed the stale "satisfy CoreDataManager dependencies" comment — those duplicates exist because the widget target doesn't pull in `Utilities/Constants.swift`). **Cross-process design (decided, no surprise):** the widget process uses `DataStore.shared` directly — `DataStore.swift` was added to both targets in Wave 1, and `ModelContainer` is process-singleton-safe (each process gets its own instance backed by the same App-Group SQLite file). `CompleteHabitIntent.perform()` is `async`, opens a per-perform `ModelContext(DataStore.shared)`, runs the toggle inline (no await needed — SwiftData contexts are thread-confined to the calling thread, no `context.perform { }` queue boundary required); finds the habit via full-fetch + in-Swift `.first(where: { $0.id == habitUUID })` (Wave 3a pattern; dodges `#Predicate`-on-optional-UUID); reads/writes today's entry through `habit.entries` (now a plain `[HabitEntry]?`, no `Set` cast); on insert, `HabitEntry(date:completed:completionState:habit:)` + `context.insert`. `HabitWidgetProvider.loadHabitsEntry` builds a fresh `ModelContext` per timeline load and runs synchronously (the framework dispatches the call itself; we don't need our own queue hop); `fetchTodaysHabits(in:)` is `FetchDescriptor<Habit>(sortBy: [SortDescriptor(\.order), SortDescriptor(\.name)])` + the existing `HabitFrequency.shouldTrack(...)` filter; `getCompletionState(for:)` is `habit.entries?.first(where:)` direct. Dropped `import CoreData` + `import CloudKit` (CloudKit was unused symbolically; CloudKit mirroring still flows through `DataStore.shared`'s `cloudKitDatabase: .automatic`). **Remaining `import CoreData`s in the entire project:** `Data/CoreDataManager.swift` + `Data/CoreDataManager+HabitDetails.swift` — both deletable now, nothing references them. **Watch-items unchanged from Wave 2/3:** (a)–(c) stand; (b) widget cross-process writes still need device-testing verification (the underlying mechanism is now SwiftData/CloudKit auto-merge rather than `NSManagedObjectContextDidSave`, but the test is the same); (d)+(e) deferred perf — full-table-fetch + in-Swift-filter applies in the widget too (timeline-load reads every Habit, then filters by frequency in Swift; OK for current N, revisit at Phase-1-ship time). **Next step = delete the stack + first build.** `git rm Data/CoreDataManager.swift Data/CoreDataManager+HabitDetails.swift` + delete the `Bullet Tracker/Bullet_Tracker.xcdatamodeld` folder; Xcode UI to drop the now-missing-file refs from the project navigator (the .xcodeproj still points at them; surface-level fix in the project file). Dustin TODO before that build: export a JSON backup from Settings (the "(b1)" in-place store-open safety net per the DataStore.swift header). Then ⌘B for the first real Xcode build of the branch — first compile-check of all the wave work — fix → device test → merge.

- 2026-05-13 — **Wave 3b done** (commit `bbe8485`; compile-checked by reading only). `Journal/DayJournalView.swift` 843→846 (+3). `DayJournalViewModel` now stores `var modelContext: ModelContext?` set in the view's `.onAppear` (the Wave-3a VM pattern, same as `JournalExportViewModel` / `SettingsViewModel`); the view also gets `@Environment(\.modelContext)` for the wire. `loadHabitEntries` / `loadNotes` / `buildExportData(for:)` all do `(try? context.fetch(FetchDescriptor<HabitEntry>()))` (and `<Note>`) followed by an in-Swift date-window filter — **no `#Predicate` yet**, mirroring Wave 3a's workaround for watch-item (e) (the `#Predicate`-on-optional-`Date?` fiddle); the day-fetch sorts by `entry.habit?.order ?? 0` in Swift since `\.habit?.order` isn't a valid `SortDescriptor` keypath. `buildExportData(for:)` fetches the two tables **once up front** (instead of per-day, the old shape) before the per-date loop. Writes: `Note(context:)` + assign-fields → `Note(date:noteDate, content:content)` + `context.insert(note)`; `context.delete(...)` API unchanged on `ModelContext`. **Watch-items unchanged from Wave 2 (a)–(e)** — Wave 3b is well inside (e)'s scope (full-table fetch on every day-tap; bounded by total notes + entries; acceptable for now). **Remaining `import CoreData`s after this commit:** `Data/CoreDataManager.swift`, `Data/CoreDataManager+HabitDetails.swift`, `BulletTrackerWidgets/BulletTrackerWidgetsBundle.swift` — first two deleted post-Wave-4; third is Wave 4 itself. **Next session = Wave 4 (widget):** read `BulletTrackerWidgetsBundle.swift` + `WidgetEnums.swift` first; rewrite `CompleteHabitIntent` + timeline provider against a `ModelContainer` built inside the extension (per-process; same App-Group store URL as `DataStore.shared`). Then delete the Core Data stack + `.xcdatamodeld` → first real Xcode build of the branch.

- 2026-05-12 — **Wave 3a done** (commit `6f787a3`; compile-checked by reading only). Reordered: Wave 3 = Journal/Notes/Settings/Backup (was Wave 4); split into **3a** (Notes/Settings/Backup/Journal-export — done) and **3b** (`DayJournalView` — pending, not yet read). Then Wave 4 = widget; then delete `CoreDataManager*`/`.xcdatamodeld`. **Pattern used for VMs that need a context but aren't `View`s** (`SettingsViewModel`, `JournalExportViewModel`): a stored `var modelContext: ModelContext?` set from the view's `.onAppear` — SwiftData contexts can't be made in a VM's `init`. Views use `@Environment(\.modelContext)` directly. **3a changes:** `Data/BackupManager.swift` 616→~430 — the `Codable` mirror structs (`BackupData`/`HabitData`/etc.) are byte-for-byte unchanged so old backups still restore; fetch/insert moved to a passed-in `ModelContext`; `clearExistingData` uses `context.delete(model:)` for all 6 types; imports use the `@Model` `init`s + `context.insert`; `JournalEntry.tags` set via array assignment (no `addToTags`). `Data/BackupRestoreViewModel.swift` — the two `Task.detached` blocks now build `ModelContext(DataStore.shared)` inside the detached task (off-main, fresh context — SwiftData-correct). `Journal/JournalJSONExporter.swift` — `exportJournalData(...in context:)`; fetch-all-then-filter for the date window (Phase-1-acceptable, see watch-item (e)). `Journal/JournalPDFGenerator.swift` 642→~620 — `generatePDF(...in modelContext:)`; **one fetch up front** (`fetchRangeData` → all completed `HabitEntry` + all `Note` in range, grouped by start-of-day) feeds the per-day loop instead of a fetch-per-day; `calculateStats(...in:)` fetches via `FetchDescriptor` + uses `HabitStore.allEntriesByDay` for the streak data (replaces the deleted `HabitCalculationService.fetchAllEntries`); all the `draw*`/`wrapText`/`estimateDayHeight` helpers untouched (already pure). `Journal/JournalExportView.swift` — VM stores `modelContext`; preview counts + PDF/JSON export go through it. `Notes/NotesView.swift` 445→~390 — `@Query var allNotes` (sorted by date desc), view filters to `viewModel.selectedDate`'s month; `NotesViewModel` slimmed to `selectedDate` + `selectedNote` + month nav (dropped `notes`/`loadNotes`); `AddNoteView`/`EditNoteView`/delete via `@Environment(\.modelContext)`. `App/SettingsView.swift` — `SettingsViewModel` stores `modelContext`; `clearAllData` uses `context.delete(model:)` + re-creates the four default collections. **Next session = Wave 3b:** `Journal/DayJournalView.swift` (843 lines) — read it first.

## Decision log

- 2026-05-12 — created from `bt-0001`'s decided plan, activated immediately (design pass already done). Scope = the ~22 Core-Data-touching `.swift` files + the model XML + the project file (over-listed deliberately; the phase-gate hook blocks edits outside scope). Build structure: one bead, 6 staged commits (vs. 6 sub-beads — rejected as over-ceremony for the size). Stage A started: `Data/Models.swift` written, added to the app + widget targets.
- 2026-05-12 — **correction (Claude's error):** the "Stage A = turn off `.xcdatamodeld` Codegen, `@Model` types drop in, migrate consumers gradually" plan was based on a wrong assumption. `@Model` classes are NOT `NSManagedObject` subclasses — legacy `NSFetchRequest<Habit>` / `Habit.fetchRequest()` / `Habit(context:)` / `.objectID` code does not compile against them. Real SwiftData↔Core Data coexistence = two parallel stacks on the same store file, models kept in sync by hand, and the Core-Data-generated classes renamed (entity name stays, "Class" name changes) so they don't collide with the `@Model` class names — i.e. either rename every Core Data class reference in the legacy code, or prefix the `@Model` classes (`SD…`) until cutover. Hacking with Swift's own guidance warns the dual-model sync "might prove rather onerous." → the gentle stage-by-stage-with-checkpoints picture doesn't hold; the realistic shape is one focused data-layer push (on a branch, JSON backup as safety net, no Core-Data-stays period). Migration *strategy* re-opened (architectural — Dustin's call); the 6-stage Plan above will be rewritten as commit-stages within one push once he picks. Current state parked buildable by un-checking `Models.swift` from both targets; resume with the chosen strategy. Source: hackingwithswift.com/quick-start/swiftdata/how-to-make-core-data-and-swiftdata-coexist-in-the-same-app.
