# Project Status

**Last updated:** 2026-05-13 (`bt-0003` **shipped** — Phase 2 of the SwiftData migration complete. All five Phase-2 deliverables landed: (1) dropped dormant journal machinery, (2) lifted `HabitEntry.date` to non-optional, (3) collapsed 3-way completion state into `CompletionStyle` + `CompletionState` enums, (4) typed the `details` JSON blob into `HabitEntryDetails` (computed accessor over a `detailsJSON: String?` storage column after Wave 3.5 SwiftData rejection), (5) rewrote 5 hot-path predicate reads with the deferred `bt-0004` perf work (+ Wave 4.5 Today-card N+1 hotfix). CloudKit re-enabled (`cloudKitDatabase: .automatic`); fresh JSON backup + iCloud-side wipe + app delete + reinstall + restore-from-backup cycle clean end-to-end. All four verification surfaces green (Today / Dashboard / Journal / Widget render correctly; force-quit + reopen persists). `swiftdata-phase2` merged to `main`. Cross-device iPad CloudKit verification deferred — bt-0002 already cross-device-verified Phase-1 schema; the bits Wave 5 carries (Phase-2 model shape) propagate the same way.)

Fast-changing state. For the "why" behind any decision, see `ARCHITECTURE.md`.

---

## Live

- Bullet Tracker iOS app (SwiftUI, **SwiftData + CloudKit**, App Group widget). Working app; Phase 2 of the SwiftData migration just shipped — model is now the cleaned-up shape (typed `HabitEntryDetails`, `CompletionStyle` + `DetailKind` enums, non-optional `HabitEntry.date`, dormant bullet-journal machinery dropped). CloudKit on; cross-device sync working.
- 2026-05-12: adopted the bead / three-doc / skill system; ran a fresh-eyes architecture read pass — see `ARCHITECTURE.md` and `bt-0001`. Patchwork flags identified (#1–#9 in session notes): three overlapping data layers, `details` JSON blob, completion state modeled 3 ways, name-keyword workout detection, half-refactored repo layout, custom Core Data vs. SwiftData, cache-thrash, calc-service concurrency, hand-rolled backup mirrors + vestigial Journal future-log fields. **8 of 9 resolved by bt-0001 → bt-0002 → bt-0003 (Phase 1 + Phase 2 of the migration);** #5 repo-layout half-refactor remains (independent hygiene, captured below).

## In progress

- _(none — Phase 2 of the SwiftData migration just shipped via `bt-0003`. App is at a clean rest point.)_

### Queued (backlog)

- _(none)_

## Next (1–3 items)

1. (Independent, anytime) Repo-layout cleanup — finish the folder refactor, delete `WidgetEnums.txt` / committed `.DS_Store`s, archive the old `Documentation/` files. Patchwork flag #5; only un-resolved item from the 2026-05-12 fresh-eyes review.
2. (Watch-item, no action) Cross-device iPad CloudKit verification of the Phase-2 schema. Deferred from Wave 5 — `bt-0002` already cross-device-verified Phase-1 sync; Phase 2's schema bits propagate the same way. Flag if anything inconsistent surfaces when iPad pickup naturally happens.

## Open questions / blockers

- ~~Data foundation: stay on custom Core Data or migrate to SwiftData?~~ — **resolved 2026-05-12: migrate to SwiftData, two-phase** (`bt-0001`).
- ~~What is the Journal "future log / migration" feature meant to do?~~ — **resolved 2026-05-13:** drop the dormant `JournalEntry`/`Collection`/`Tag` machinery entirely (leftover from the original bullet-journal vision, never wired to UI; live Journal tab is on `Note`). Scope folded into `bt-0003`; reading the code surfaced that the issue was the whole dormant model, not just the 7 fields the patchwork-flags-review captured.

## Recent build (last 3)

_(Newest on top. When a 4th lands, oldest migrates to `ARCHITECTURE.md` → Build Log.)_

- `bt-0003` (shipped 2026-05-13) — **SwiftData Phase 2 — model restructure complete.** 8 waves on `swiftdata-phase2` branch, merged to `main` at ship. **Five Phase-2 deliverables all landed:** (1) dropped dormant journal machinery (`Collection` / `JournalEntry` / `Tag` `@Model` types + 7 vestigial future-log fields + backup mirrors + half-built collection UI, Wave 1, −259 lines); (2) lifted `HabitEntry.date: Date? → Date` non-optional (Wave 2); (3) collapsed 3-way completion state — added `CompletionStyle` + `DetailKind` enums on `Habit`, replaced `completed: Bool` + `completionState: Int16` with typed `CompletionState` on `HabitEntry` (Wave 2); (4) typed the `details` JSON blob — `HabitEntryDetails` discriminated enum exposed as a computed property over a stored `detailsJSON: String?` column after Wave 3.5 SwiftData rejection of `Codable`-enum-as-attribute (same bt-0004 fragility class); (5) rewrote 5 hot-path predicate reads (the deferred `bt-0004` perf work) with the non-optional date unblocking it, +66 lines + Wave 4.5 Today-card N+1 hotfix +24 lines. Migration vehicle was Path 3 (wipe + restore via `BackupManager`) — proven during Wave 1 recovery from a schema-shrink + CloudKit `HistoryExpired` interaction; Wave 5 ship re-ran the same cycle (fresh backup → iCloud wipe → CloudKit re-enable → app delete + reinstall → restore) cleanly end-to-end. All four verification surfaces green (Today / Dashboard / Journal / Widget render + persist). Cross-device iPad CloudKit verification deferred — bt-0002 already cross-device-verified Phase-1 sync; Phase 2's schema bits propagate the same way. Full Decision log in `bt-0003` including all 8 waves + the Wave 1 → Recovery / Plan pivot.
- `bt-0004` (shipped 2026-05-13) — **SwiftData data-layer perf pass — indexes-only ship; predicate rewrite deferred to `bt-0003`.** Original four-decision plan: (1) hot-path predicate rewrite, (2) date indexes, (3) cold paths untouched, (4) no `ModelActor`. Build phase surfaced that SwiftData's `#Predicate` on this iOS version rejects every shape of optional-`Date` comparison tried (force-unwrap, inline nil-coalesce, captured-local sentinel) — `Date?` would need to become `Date`, which requires a SwiftData `VersionedSchema` migration against the live CloudKit-synced store. That belongs in `bt-0003`'s model-restructure surface, not as a one-off scope expansion here. Net shipped: two `#Index` lines in `Data/Models.swift` (`HabitEntry.date` + `JournalEntry.date`) — re-creates the `byDateIndex` fetch index the original `.xcdatamodeld` had, making any future date-filtered query cheap (including `bt-0003`'s eventual predicate rewrite). Reverted predicate-rewrite attempts in `HabitDataRepository.swift` and `BulletTrackerWidgetsBundle.swift` back to pre-bt-0004 state (kept in `scope_files` as authorization record). Smoke build clean; device-validation on next app-open (schema change is index-add only, no new entities — Apple docs say lightweight `CREATE INDEX` on first launch). Full Decision log in `bt-0004` including the design pass, the Decision-2-was-wrong correction caught mid-build, and the scope-pivot reasoning.
- `bt-0002` (shipped 2026-05-13, merge `ba1edde`) — **SwiftData migration Phase 1 — engine swap, model shape unchanged.** Replaced the hand-rolled Core Data stack (`NSPersistentCloudKitContainer` + `CoreDataManager` + `HabitDataRepository` cache + `@unchecked Sendable` calc service + the `NSManagedObjectContextDidSave` merge listener) with `@Model` types behind one `ModelContainer` (`DataStore.shared`) sharing the existing App-Group SQLite store in place; CloudKit sync continues via `cloudKitDatabase: .automatic`. Six waves on the `swiftdata-migration` branch: baseline `@Model` types → DataStore wiring → Habits surface (flags #4 + #8 folded) → Notes/Settings/Backup/Journal-export → DayJournalView → widget extension → delete the stack. **Net: +1387 / −2070 = 683 fewer lines** across 35 files for the same functionality. Device-tested 1-5 of 6 (cross-device CloudKit verified 2026-05-13 — iPad pickup of 3-4 same-day habit completions; backup round-trip still deferred, low-risk). Full Decision log in `bt-0002`.
